meta {
  name: "User - Delete"
  type: http
  seq: 25
}

delete {
  url: {{BaseUrl}}/api/v1/User/1
  auth: inherit
}

vars:pre-request {
  BaseUrl: http://localhost:5210
}

settings {
  encodeUrl: true
  timeout: 0
}