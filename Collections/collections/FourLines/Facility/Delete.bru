meta {
  name: "Facility - Delete"
  type: http
  seq: 10
}

delete {
  url: {{BaseUrl}}/api/v1/Facility/1
  auth: inherit
}

vars:pre-request {
  BaseUrl: http://localhost:5210
}

settings {
  encodeUrl: true
  timeout: 0
}