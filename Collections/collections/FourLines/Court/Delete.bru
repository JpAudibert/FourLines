meta {
  name: "Court - Delete"
  type: http
  seq: 5
}

delete {
  url: {{BaseUrl}}/api/v1/Court/1
  auth: inherit
}

vars:pre-request {
  BaseUrl: http://localhost:5210
}

settings {
  encodeUrl: true
  timeout: 0
}