meta {
  name: "Sport - Delete"
  type: http
  seq: 20
}

delete {
  url: {{BaseUrl}}/api/v1/Sport/1
  auth: inherit
}

vars:pre-request {
  BaseUrl: http://localhost:5210
}

settings {
  encodeUrl: true
  timeout: 0
}